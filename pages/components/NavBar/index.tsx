import React from "react";

const NavBar: React.FC = () => {
  return (
    <nav
      style={{
        position: "sticky",
        display: "flex",
        justifyContent: "space-between",
        alignItems: "center",
        top: 0,
        left: 0,
        right: 0,
        height: 80,
        padding: "0 40px",
        background: "#F4F5FF",
      }}
    >
      {/* empty */}
    </nav>
  );
};

export default NavBar;
